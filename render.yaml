services:
  - type: web
    name: arc-network
    env: node
    plan: free
    rootDir: backend
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
